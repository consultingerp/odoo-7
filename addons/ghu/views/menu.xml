<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">
        <!-- menu categories -->
        <record model="ir.actions.act_window" id="ghu.student_window">
            <field name="name">GHU Students</field>
            <field name="res_model">ghu.student</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_type">form</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="domain">[]</field>
            <field name="context">{}</field>
        </record>

        <menuitem name="Students" id="ghu.students" parent="ghu.menu_ghu" action="ghu.student_window"/>

    </data>
</odoo>
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <template id="view_payment_term_form" inherit_id="account.view_payment_term_form">
            <field name="note" position="after">
                <field name="additional_fee"/>
            </field>
            <field name="additional_fee" position="after">
                <field name="form_label"/>
            </field>
            <field name="form_label" position="after">
                <field name="form_description"/>
            </field>
        </template>
    </data>
</odoo>
